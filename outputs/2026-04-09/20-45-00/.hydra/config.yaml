data:
  dataset: circuitnet
  data_root: /tmp/circuitnet_work/instance_placement/
  label_root: /home/jovyan/shared/ctrl-nxp/CircuitNet-N28/training_set/congestion/label/
  num_workers: 8
  batch_size: 8
experiment:
  project: circuitformer
  save_dir: /home/jovyan/CircuitFormer/exp/finetune_n28_run1
  checkpoint_interval: 1000
  log_image_interval: 500
  ckpt_path: /home/jovyan/CircuitFormer/ckpts/circuitformer.ckpt
model:
  model_name: circuitformer
  loss: mse
  max_epochs: 20
  warmup_epochs: 2
  lr_scheduler: cosine
  lr: 0.0001
  warmup_lr: 1.0e-06
  min_lr: 1.0e-06
  weight_decay: 0
  batch_size: 8
  loss_weight: 128
  label_weight: 1
trainer:
  max_epochs: 20
  log_every_n_steps: 1
  accelerator: auto
  devices: auto
  precision: 32
  check_val_every_n_epoch: 1
  sync_batchnorm: false
  strategy: auto
